# Quickstart Validation

1. Run the availability widget contract test and confirm default and filtered selectors.
2. Run the latency widget contract test and confirm successful-status scope, weighted mean, seconds unit, and selected period.
3. Run the SLA alert contract test and confirm both expressions, zero-traffic guards, and rule-specific annotations.
4. Run recursive formatting checks.
5. Initialize and validate the three touched modules if provider/module caches are available.
